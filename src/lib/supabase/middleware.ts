// Minimal middleware shim for existing imports. Implement proper session update
// logic with Firebase Auth if/when converting middleware.
export async function updateSession() {
  // no-op shim for now
  return;
}
