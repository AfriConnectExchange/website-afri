
// Placeholder file left intentionally empty so the JSX-capable route file
// `route.tsx` can be used by the Next.js router. This avoids duplicate
// TypeScript/JSX parsing issues while migrating the old .ts -> .tsx.

export const __placeholder = true;
